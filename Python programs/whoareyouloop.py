while True:
    name = input ('Who are you ')
    if name != 'Armin':
        continue
    password = input ('What is the password Armin? ')
    if password == 'roki':
        print ("That's correct")
        break